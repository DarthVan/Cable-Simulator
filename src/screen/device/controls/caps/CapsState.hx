package screen.device.controls.caps;

/**
 * ...
 * @author Sith
 */

enum abstract CapsState(String) to String {

	var OPENED = 'opened';
	var CLOSED = 'closed';
	var CLOSED_LABELED = 'closed_labeled';
	
}