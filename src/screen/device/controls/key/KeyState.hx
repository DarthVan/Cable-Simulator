package screen.device.controls.key;

/**
 * ...
 * @author Sith
 */

enum abstract KeyState(String) to String {

	var LEFT = 'left';
	var LEFT_NO_KEY = 'left_no_key';
	var CENTER = 'center';
	var CENTER_NO_KEY = 'center_no_key';
	
}