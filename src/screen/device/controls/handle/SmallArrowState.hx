package screen.device.controls.handle;

/**
 * ...
 * @author Sith
 */

enum abstract SmallArrowState(String) to String {
	
	var UP = 'up';
	var MIDDLE = 'middle';
	var DOWN = 'down';
	
}