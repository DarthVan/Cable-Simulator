package screen.device.controls.handle;

/**
 * @author Sith
 */

enum abstract HandleState(String) to String {
	
	var LEFT = 'left';
	var UP = 'up';
	var RIGHT = 'right';
	var NONE = 'none';
	
}