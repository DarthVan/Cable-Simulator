package screen.device.tools;

/**
 * @author Sith
 */

enum abstract ToolType(String) to String {
	
	var HANDLE = 'handle';
	var PADLOCK = 'padlock';
	var KEY = 'key';
	var TABLE = 'table';
	
}